Hosted on Cloudflare Pages: https://ward-tools.org

## Email Delivery

MailChannels email sending requires `APP_ORIGIN` and `MAILCHANNELS_API_KEY` in the Worker environment. The sender domain also needs the MailChannels DNS setup described in the Email API docs.